package dev.markodojkic.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class JasyptConfig {
    // Jasypt Spring Boot auto-configuration is enabled by the starter.
    // Configure JASYPT_ENCRYPTOR_PASSWORD in the environment for ENC(...) values.
}
